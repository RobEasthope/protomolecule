{
  "name": "@robeasthope/github-rulesets",
  "version": "1.0.2",
  "private": true,
  "description": "GitHub repository configuration and rulesets for consistent branch protection",
  "license": "MIT",
  "files": [
    "rulesets"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "format": "npx prettier --write .",
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix",
    "sort-pkg-json": "npx sort-package-json"
  },
  "devDependencies": {
    "@robeasthope/tsconfig": "workspace:*",
    "rimraf": "^6.0.1"
  }
}
