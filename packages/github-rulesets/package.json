{
  "name": "@protomolecule/github-rulesets",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub configuration rulesets for Protomolecule projects",
  "files": [
    "rulesets"
  ],
  "scripts": {
    "clean": "rm -rf node_modules",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "@protomolecule/tsconfig": "workspace:*"
  }
}
