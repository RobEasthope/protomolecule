{
  "name": "@protomolecule/tsconfig",
  "version": "1.0.0",
  "license": "MIT",
  "private": false,
  "files": [
    "base.json",
    "react-library.json",
    "node.json"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "format": "npx prettier --write .",
    "sort-pkg-json": "npx sort-package-json"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
