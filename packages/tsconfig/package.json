{
  "name": "@protomolecule/tsconfig",
  "version": "1.0.0",
  "private": true,
  "files": [
    "base.json",
    "react-library.json",
    "node.json"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
